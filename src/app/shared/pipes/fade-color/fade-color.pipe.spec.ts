import { FadeColorPipe } from './fade-color.pipe';

describe('FadeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new FadeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
