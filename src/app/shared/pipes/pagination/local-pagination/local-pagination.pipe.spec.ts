import { LocalPaginationPipe } from './local-pagination.pipe';

describe('LocalPaginationPipe', () => {
  it('create an instance', () => {
    const pipe = new LocalPaginationPipe();
    expect(pipe).toBeTruthy();
  });
});
